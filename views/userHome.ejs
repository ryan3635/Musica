<div id="feedFlex">
    <%- include("header") %>

    <div id="toolbar">
        <p>Musica</p>
        <a href="/albumSearch" class="buttonStyle">Album Search</a> &nbsp &nbsp
        <a href="/userProfile/<%=list%>?page=1" class="buttonStyle">Your List</a> &nbsp &nbsp
        <a href="/account" class="buttonStyle">Account</a> &nbsp &nbsp
        <a href="/logout" class="buttonStyle">Log out</a>
    </div>
    
    <div id="header">
        <% if (loggedIn) { %>
            <p class="message hidden">Successfully Logged In!</p>
        <% } %>

        <% if (googleDisplayname) { %>
            <p class="message hidden">Display Name Updated!</p>
        <% } %>
        <h2>Your Feed</h2>
        <hr style="width: 18%; border-width: 1.9px; margin-top: 2px">
    </div>

    <div id="homeFlex">
        <!-- update home when someone creates a list, updates a list, shares a list publicly -->
        <!-- for now, have same feed for logged in and non-logged in users -->
        <!-- have a maximum of 50? posts and then delete ones that are old -->
        <!-- need new collection for public home feed -->
        <!-- each document within the collection is one post on the home page -->
        <!-- each has a special "post type", i.e. added to list, create list, share list, followed by the contents of the post (description, album art + info) -->

        <!-- for each item in public collection -->
        <div id="homeEntry">
            <p style="font-size: 1.5rem; font-weight: bold; font-style: italic; text-decoration: underline;">Ryan updated their list!</p>

        </div>
        <!-- for list creation, show first 10 album covers as little blocks -->
        <!-- for single album adds, show album cover slighty bigger and give year + info -->
        <!-- get img source, and album info (artist, title, year) -->
    </div>

    <%- include("footer") %>
</div>
